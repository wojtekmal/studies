#include <iostream>
using namespace std;

int64_t descreet_log(int64_t num, int64_t base, int64_t mod)
{
    // Find an exponent p such that mod | base^p - num or return -1 if such a
    // number doesn't exist.
}

int main()
{
    int64_t num, base, mod;
    cout << discreet_log(num, base, mod) << "\n";
}